name: My First Workflow


on: push


jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My First Github Actions Job"

        - name: List files
          run: ls

        - name: Read file
          run: cat actions-1/README.md